# 知识库组织说明

本系统支持将企业制度文档按业务主题放入不同子目录中统一管理。

- `documents/根目录` 可放置员工普遍可见的基础制度文件。
- `documents/人力资源制度` 可放置招聘、绩效、员工档案等敏感制度。
- `documents/财务管理制度` 可放置报销、差旅、票据规范等财务类文件。
- `documents/行政管理制度` 可放置办公规范、资产领用、会议管理等行政类文件。

索引重建后，系统会递归读取子文件夹中的 `txt`、`md`、`pdf` 和 `docx` 文件，并根据文档名称或所在目录自动补充权限标签，从而支持按用户身份限制检索范围。
